# Host-a node-exporter environment — replaces the base service file
# byte-for-byte at /etc/node-exporter/node-exporter.env.
NODE_EXPORTER_LISTEN=:9100
NODE_EXPORTER_COLLECTORS=cpu,meminfo,filesystem,netdev,systemd,processes
