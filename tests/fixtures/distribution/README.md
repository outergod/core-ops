# Distribution Fixtures

These fixtures define the public distribution-readiness contract inputs used by
integration tests.

- `entrypoint-snapshot.md` captures the expected stable entrypoint structure.
- `release-gate-environment.json` captures the authoritative verification
  environment identity contract.
- `release-metadata.json` captures the credibility-surface source values used by
  public distribution tests.
