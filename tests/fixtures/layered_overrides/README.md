# Layered Overrides Fixture

This fixture mirrors the layered overrides repository layout used by the feature.
It contains base services under `services/` and host-specific selections plus
overrides under `hosts/<host>/`.
