[Container]
# Hardware accel via Intel/AMD VAAPI on /dev/dri. For NVIDIA, replace
# with `AddDevice=nvidia.com/gpu=all` (CDI) or
# `PodmanArgs=--device nvidia.com/gpu=all`. Out of scope here; choose
# the right shape for the host's accelerator.
AddDevice=/dev/dri:/dev/dri
