# Command Templates

This directory is reserved for Specify command templates.

Add `*.md` command templates here when you define custom commands for your
workflow. Keep them aligned with the project constitution.
